# section information
section:
  name: Outreach
  id: recent-posts
  enable: true
  weight: 9
  showOnNavbar: true
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
